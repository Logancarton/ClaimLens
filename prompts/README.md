# Prompts

Prompts define model tasks such as evidence extraction, claim challenge, and result explanation.

A prompt may interpret unstructured language but must not become the authoritative source of deterministic billing requirements.

When prompt behavior becomes important to benchmark results, version changes deliberately and test them against frozen cases.
